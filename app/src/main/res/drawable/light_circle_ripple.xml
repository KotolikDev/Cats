<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/circle_ripple.xml -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#66FFFFFF"> <!-- Цвет эффекта -->

    <!-- Маска (обязательно!) -->
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white"/> <!-- Цвет не важен -->
            <!-- Фиксируем круглый размер -->
            <size
                android:width="100dp"
            android:height="100dp"/>
        </shape>
    </item>

    <!-- Видимая подложка круга -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#10FFFFFF"/> <!-- Светлый фон -->
            <size
                android:width="100dp"
                android:height="100dp"/>
        </shape>
    </item>
</ripple>